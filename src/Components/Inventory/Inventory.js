import React from 'react';
import useProducts from '../../hooks/useproducts';

const Inventory = () => {
  
    
     
    return (
        <div>
            <h1>Here only for the inventory{}</h1>
        </div>
    );
};

export default Inventory;